Let $f$ be continuous real function, strictly increasing in an interval $[0,
a]$ such that $f(0) = 0$. Let $g$ be the inverse of $f$, i.e., $g(f(x)) = x$
for all $x$ in $[0, a]$. Show that for $0 \le x \le a$, $0 \le y \le f(a)$, we
have $$ xy \le \int_{0}^{x} f(t) dt + \int_{0}^{y} g(t) dt. $$
