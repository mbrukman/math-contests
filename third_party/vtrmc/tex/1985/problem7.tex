Let $f = f(x)$ be a real function of a real variable which has continuous third
derivative and which satisfies, for a given $c$ and all real $x$, $x \ne c$,

$$
\frac{f(x)− f(c)}{x - c} = \frac{f'(x) + f'(c)}{2} .
$$

Show that $\displaystyle{ f(x) = \frac{f'(x - f'(c))}{x−c} }$.
