Let $P(x) = x^{100} + 20x^{99} + 198x^{98} + a_{97} x^{97} + \dots + a_1 x + 1$
be a polynomial where the $a_i$ ($1 \leq i \leq 97$) are real numbers. Prove
that the equation $P(x) = 0$ has at least one complex root (i.e., a root of the
form $a + bi$ with $a, b$ real numbers and $b \ne 0$).
