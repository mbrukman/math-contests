Let $f(x, y) = \ln(1 - x^2 - y^2) - \dfrac{1}{(y - x)^2}$ with domain $D =
\lbrace (x, y) | x \ne y, x^2 + y^2 < 1 \rbrace$. Find the maximum value $M$ of
$f(x, y)$ over $D$. You have to show that $M \ge f(x, y)$ for every $(x, y) \in
D$. Here $\ln(\cdot)$ is the natural logarithm function.
