Define $f(x, y) = \dfrac{xy}{x^2 + (y \ln(x^2))^2}$ if $x \ne 0$, and $f(0, y)
= 0$ if $y \ne 0$. Determine whether $\displaystyle{\lim_{(x,y) \to (0,0)} f(x,
y)}$ exists, and what its value is if the limit does exist.
