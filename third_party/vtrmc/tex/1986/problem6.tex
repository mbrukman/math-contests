Sets $A$ and $B$ are defined by $A = \lbrace 1, 2, ..., n \rbrace$ and
$B = \lbrace 1, 2, 3 \rbrace$. Determine the number of distinct functions from
$A$ onto $B$.

Recall that a function $f : A \rightarrow B$ is "onto" if for each $b \in B$,
there exists $a \in A$ such that $f(a) = b$.
