Let $A, B \in M_6 (\mathbb{Z})$ such that $A \equiv I \equiv B \bmod 3$ and
$A^3 B^3 A^3 = B^3$. Prove that $A = I$. Here $M_6 (\mathbb{Z})$ indicates the
6 by 6 matrices with integer entries, $I$ is the identity matrix, and $X \equiv
Y \bmod 3$ means all entries of $X - Y$ are divisible by 3.
