Let $n$ be a positive integer and let $A$ be an $n \times n$ matrix with real
numbers as entries. Suppose $4A^4 + I = 0$, where $I$ denotes the identity
matrix. Prove that the trace of $A$ (i.e., the sum of the entries on the main
diagonal) is an integer.
