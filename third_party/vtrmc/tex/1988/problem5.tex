Let $f$ be differentiable on $[0, 1]$ and let $f(\alpha) = 0$ and $f(x_0) =
-.0001$ for some $\alpha$ and $x_0 \in (0, 1)$. Also let $|f'(x)| \ge 2$ on
$[0, 1]$. Find the smallest upper bound on $|\alpha - x_0|$ for all such
functions.
