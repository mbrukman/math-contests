For a positive integer $a$, let $P(a)$ denote the largest prime divisor of
$a^2 + 1$. Prove that there exist infinitely many triples $(a, b, c)$ of
distinct positive integers such that $P(a) = P(b) = P(c)$.
