Let $f$ be defined on the natural numbers as follows: $f(1) = 1$ and for $n >
1$, $$f(n) = f(f(n - 1)) + f(n - f(n - 1))$$

Find, with proof, a simple explicit expression for $f(n)$ which is valid for
all $n = 1, 2, \dots$.
