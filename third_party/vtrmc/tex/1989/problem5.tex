\begin{enumerate}[(a)]

\item Prove that $f_0(x) = 1 + x + x^2 + x^3 + x^4$ has no real zero.
\item Prove that, for every integer $n \ge 0$, $f_n(x) = 1+2^{-n} x+3^{-n} x^2
      +4^{-n} x^3 + 5^{-n} x^4$ has no real zero. (Hint: consider $(d/dx)(x
      f_n(x))$.)

\end{enumerate}
