Let $g$ be defined on $(1, \infty)$ by $g(x) = x/(x - 1)$, and let $f^k(x)$ be
defined by $f^0(x) = x$ and for $k > 0$, $f^k(x) = g(f^{k-1}(x))$.

Evaluate $\displaystyle{\sum_{k=0}^{\infty} 2^{-k} f^k(x)}$ in the form
$\dfrac{ax^2 + bx + c}{dx + e}$.
