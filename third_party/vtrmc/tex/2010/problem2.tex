For $n$ a positive integer, define $f_1 (n) = n$ and then for $i$ a positive
integer, define $f_{i+1} (n) = f_i (n)^{f_i (n)}$. Determine $f_{100} (75)
\bmod 17$ (i.e. determine the remainder after dividing $f_{100} (75)$ by 17, an
integer between 0 and 16). Justify your answer.
