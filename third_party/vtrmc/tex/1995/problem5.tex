Let $\mathbb{R}$ denote the real numbers, and let $\theta : \mathbb{R} \to
\mathbb{R}$ be a map with the property that $x > y$ implies $(\theta(x))^3 >
\theta(y)$. Prove that $\theta(x) > -1$ for all $x$, and that $0 \le \theta(x)
\le 1$ for at most one value of $x$.
