Let $a_n = frac(1 dot 3 dot 5 dot ... dot (2n - 1),
                2 dot 4 dot 6 dot ... dot 2n)$.

#set enum(numbering: "(a)")

+ Prove that $lim_(n -> infinity) a_n$ exists.
+ Show that $a_n = frac((1 - (frac(1, 2))^2)
                        (1 - (frac(1, 4))^2)
                        dots
                        (1 - (frac(1, 2n))^2),
                        (2n + 1) a_n)$.
+ Find $lim_(n -> infinity) a_n$ and justify your answer.
