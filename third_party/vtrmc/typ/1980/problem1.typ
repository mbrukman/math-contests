#let astr = $ast.op$

Let $astr$ denote a binary operation on a set $S$ with the property that

$
  (w astr x) astr (y astr z) = w astr z "for all" w, x, y, z in S
$

Show:

#set enum(numbering: "(a)")

+ If $a astr b = c$, then $c astr c = c$.
+ If $a astr b = c$, then $a astr x = c astr x$ for all $x in S$.
