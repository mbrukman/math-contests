Let $f(x, y) = frac(x + y, 2), g(x, y) = sqrt(x y), h(x, y) = frac(2 x y, x + y)$, and let

$ S = { (a, b) in NN times NN divides a != b " and " f(a,b), g(a,b), h(a,b) in NN }, $

where $NN$ denotes the positive integers. Find the minimum of $f$ over $S$.
