#image("vtrmc-2000-problem4.svg")

In the above diagram, $l_1 = overline(A B)$, $l_2 = overline(A C)$, $x =
overline(B P)$, and $l = overline(B C)$, where $overline(A B)$ indicates the
length of $A B$. Prove that $l_2 - l_1 = display(integral_0^l
cos(theta(x)) dif x)$.
