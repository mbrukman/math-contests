Define $F(x)$ by $F(x) = sum_(n=0)^infinity F_n x^n$ (wherever
the series converges), where $F_n$ is the $n$th Fibonacci number defined by
$F_0 = F_1 = 1, F_n = F_(n - 1) + F_(n - 2), n > 1$.  Find an explicit
closed form for $F(x)$.
