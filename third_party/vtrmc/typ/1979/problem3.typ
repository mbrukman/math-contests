Let $A$ be an $n times n$ nonsingular matrix with complex elements, and let
$overline(A)$ be its complex conjugate. Let $B = A overline(A) + I$, where $I$
is the $n times n$ identity matrix.

Prove or disprove:

#set enum(numbering: "(a)")
+ $A^(-1) B A = overline(B)$
+ The determinant of $A overline(A) + I$ is real.
