#let circ = $circle.stroked.tiny$

Let $triangle.t A B C$ be a triangle with sides $a, b, c$ and corresponding
angles $A, B, C$ (so $a = B C$ and $A = angle B A C$ etc.). Suppose that $4A +
3C = 540^circ$. Prove that $(a - b)^2 (a + b) = b c^2$.
