Let ${a_n}$ be a monotonic decreasing sequence of positive real numbers with
limit 0 (so $a_1 >= a_2 >= ... >= 0$). Let ${ b_n }$ be a rearrangement of the
sequence such that for every non-negative integer $m$, the terms $b_(3m+1),
b_(3m+2), b_(3m+3)$ are a rearrangement of the terms $a_(3m+1), a_(3m+2),
a_(3m+3)$ (thus, for example, the first 6 terms of the sequence ${ b_n }$ could
be $a_3, a_2, a_1, a_4, a_6, a_5$). Prove or give a counterexample to the
following statement: the series $display(sum_(n=1)^(infinity) (-1)^n b_n)$ is
convergent.
