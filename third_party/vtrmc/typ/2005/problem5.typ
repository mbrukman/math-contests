Define $f(x, y) = frac(x y, x^2 + (y ln(x^2))^2)$ if $x != 0$, and $f(0, y) =
0$ if $y != 0$. Determine whether $display(lim_((x,y) -> (0,0)) f(x, y))$
exists, and what its value is if the limit does exist.
