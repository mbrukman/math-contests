The number of individuals in a certain population (in arbitrary real units)
obeys, at discrete time intervals, the equation

$ y_(n+1) = y_n (2 - y_n) quad"for " n = 0, 1, 2, ..., $

where $y_0$ is the initial population.

#set enum(numbering: "(a)")

+ Find all "steady-state" solutions $y^*$ such that, if $y_0 = y^*$, then
  $y_n = y^*$ for $n = 1, 2, ...$.
+ Prove that if $y_0$ is any number in $(0, 1)$, then the sequence ${ y_n }$
  converges monotonically to one of the steady-state solutions found in (a).
