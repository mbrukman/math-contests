Let $m, n$ be integers such that $n >= m >= 1$. Prove that $display(frac(gcd(m,
n), n) binom(n, m))$ is an integer. Here $gcd$ denotes greatest common divisor
and $display(binom(n, m) = frac(n!, m!(n-m)!))$ denotes the binomial
coefficient.
