Let $n$ be a nonnegative integer and let $f(x) = a_n x^n + a_(n-1) x^(n-1) +
... + a_1 x + a_0 in RR[x]$ be a polynomial with real coefficients
$a_i$. Suppose that

$ (a_n)/((n+1)(n+2)) + (a_(n-1))/(n(n+1)) + ... + (a_1)/(6) + (a_0)/(2) = 0. $

Prove that $f(x)$ has a real zero.
