Sets $A$ and $B$ are defined by $A = { 1, 2, ..., n }$ and
$B = { 1, 2, 3 }$. Determine the number of distinct functions from
$A$ onto $B$.

Recall that a function $f : A -> B$ is "onto" if for each $b in B$,
there exists $a in A$ such that $f(a) = b$.
