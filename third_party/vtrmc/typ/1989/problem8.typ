The integer sequence ${ a_0, a_1, ..., a_(n-1) }$ is such that,
for each $i$ ($0 <= i <= n - 1$), $a_i$ is the number of $i$'s in the
sequence. (Thus for $n = 4$ we might have the sequence ${ 1, 2, 1, 0
}$.)

#set enum(numbering: "(a)")

+ Prove that, if $n >= 7$, such a sequence is a unique.
+ Find such a sequence for $n = 7$.

Hint: show that the sum of all the terms is $n$, and that there are $n - a_0 -
1$ nonzero terms other than $a_0$ which sum to $n - a_0$. (This problem is
slightly modified from one on the Cambridge Men's Colleges Joint Awards and
Entrance Examination, 24 November 1970.)
