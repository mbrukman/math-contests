Let $g$ be defined on $(1, infinity)$ by $g(x) = x/(x - 1)$, and let $f^k(x)$ be
defined by $f^0(x) = x$ and for $k > 0$, $f^k(x) = g(f^(k-1)(x))$.

Evaluate $display(sum_(k=0)^(infinity) 2^(-k) f^k(x))$ in the form
$frac(a x^2 + b x + c, d x + e)$.
