Evaluate $display(integral_0^x frac(dif theta, 2 + tan(theta))),$ where
$0 <= x <= pi/2$.

Use your result to show that $display(integral_0^(pi/4) frac(dif theta, 2 +
tan(theta)) = frac(pi + ln(9/8), 10))$.
