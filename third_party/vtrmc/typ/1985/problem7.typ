Let $f = f(x)$ be a real function of a real variable which has continuous third
derivative and which satisfies, for a given $c$ and all real $x$, $x != c$,

$ (f(x) - f(c))/(x - c) = (f'(x) + f'(c))/2 . $

Show that $f(x) = display(f'(x - f'(c)))/(x-c)$.
