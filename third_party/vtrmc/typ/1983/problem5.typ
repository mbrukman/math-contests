Find the function $f(x)$ such that for all $L >= 0$, the area under the graph of
$y = f(x)$ and above the $x$-axis from $x = 0$ to $x = L$ equals the arc length of
the graph from $x = 0$ to $x = L$. Hint: recall that
$d/d x cosh^(-1) x = 1/sqrt(x^2 - 1)$.
