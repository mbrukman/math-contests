Let $CC$ denote the complex numbers and let $M_3(CC)$ denote the 3 by 3
matrices with entries in $CC$. Suppose $A, B in M_3(CC)$, $B eq.not 0$, and $A
B = 0$ (where $0$ denotes the 3 by 3 matrix with all entries zero). Prove that
there exists $0 eq.not D in M_3(CC)$ such that $A D = D A = 0$.
